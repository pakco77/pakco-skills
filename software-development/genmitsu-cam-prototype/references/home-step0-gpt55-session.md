# Genmitsu CAM GPT-5.5 接管会话要点（2026-05-10）

## 触发场景
用户要求在 `/Users/pakco/Desktop/Claude + CAM/0_Agents team` 接管 Claude 已讨论/产出的 Genmitsu CAM Agent Team，并回到 PMO 苏格拉底，而不是直接继续 FE。

## 新增稳定裁决
- 最终原型文件不得覆盖旧版 `原型.html`，需另存为：
  `/Users/pakco/Desktop/Claude + CAM/0_Agents team/2_B_迭代冻结产出/原型(GPT5.5).html`
- Home 首页 = 新手引导 Step 0。
  - 保留「我的项目 + 社区作品 mock」两板块。
  - Home 主 CTA 承担首雕入口职责。
- Home CTA 层级：
  - 主按钮：「开始第一次雕刻」
  - 弱入口：「新建空白项目」
  - 目的：主路径服务新手首雕；弱入口服务熟手/跳过引导用户。
- 首雕 Wizard 当前冻结前 2 步：
  - Step 1：确认机型 / 刀具 / 材料
  - Step 1 形态：混合式确认卡
  - Step 1 默认值：Cubiko / 1/8 20° V刀 / 木板
  - Step 1 弱入口：「更改配置（即将支持）」
  - Step 2：确认并进入画布
  - Step 2 不再单独选模板，默认直接带入「铭牌」模板
  - Step 2 主按钮文案锁定为「进入画布」
- UI 启用 Open Design 参考时，推荐风格组合：
  - 以 `notion` 作为白底主基调（温暖极简、轻边框、柔和层次）
  - 借 `linear-app` 的排版层次、信息密度、精确感（只借层次与排版，不借暗色与紫色）
  - 少量借 `stripe` 的精致表单/边框/专业感
  - 最终统一替换为 Genmitsu 绿 VI，形成「高级浅色底 + 工业精度感 + 专业工具」方向

## 接管流程经验
1. 先读 PMO 全局日志：
   `2_B_迭代冻结产出/0_迭代冻结产出/_product/log.md`
2. 再读当前 feature 文件夹，确认已完成阶段，不要从 0 重新跑。
3. 用户选择“回到 PMO 苏格拉底”时，不派发下游 Agent；每次只问一个产品方向裁决问题。
4. 每个裁决立即追加到 `_product/log.md`，并标注影响范围（如 `L1/F000/F011 待补丁`）。
5. 若用户指定新原型文件名，后续 FE/FQA/VQA 都应以新文件为目标，旧 `原型.html` 只作参考。

## easyCrave 对首雕路径的参考
来自 `0_S_背景级资料/003_S_市场调研/easyCrave.md`：
- easyCrave 核心不是专业 CAM，而是“内容模板 + 引导式 CAM + G-code 导出”。
- 主链路强调 Quick Start / 模板 / 材料 / 机器 / 刀具 / 后处理 / 导出检查。
- 对 Genmitsu 的启发：首雕路径应优先通过选择题收齐关键条件，避免新手直接面对空白画布。

## 后续补丁影响
这些裁决会影响：
- L1 产品级 Roadmap
- F000 Home PRD / UX / UI
- F011 新手覆盖层引导剧本
- 可能新增或重命名首雕设置页相关 L2 feature（由 PMO/DA 决定）

## 补丁方案整理阶段（2026-05-10 19:50）

### 触发条件
PMO 苏格拉底已收束，不再继续问产品方向问题；但 L1 / F000 / F011 的冻结文档仍沿用旧口径，需要先整理补丁输入再派发正式修订。

### 当次扫描结论
- L1 仍是旧口径：F000 还是“Home + 项目入口”，F011 还是“工作台覆盖层引导”。
- F000 PRD / UX / UI 仍围绕「+ 新建项目 → 直接跳 workspace」展开。
- 当前冻结产出中未发现 F011 文档，因此补丁摘要还承担 F011 首次建档输入基线。

### 标准动作
1. 先扫描 `_product/log.md`、L1 Roadmap、F000 当前冻结文档，确认哪些地方仍是旧口径。
2. 输出 `_product/YYYY-MM-DD-L1-F000-F011-...-patch-summary.md`，把已锁裁决整理成可派发补丁摘要。
3. 立即在 `_product/log.md` 追加一条 `生成补丁摘要` 记录，标明作用范围与文件路径。
4. 再按 `L1 → F000 PRD → F000 UX → F000 UI → F011` 的顺序派发正式文档修订。

### 要点
- 补丁摘要阶段的目标不是直接重写全部文档，而是先统一口径，避免下游继续沿用旧的 `home.new-project` / `直接进 workspace` 叙事。
- 若 F011 尚未建档，补丁摘要必须显式写出这一点，并把自己定位成 F011 首次建档的输入基线。
